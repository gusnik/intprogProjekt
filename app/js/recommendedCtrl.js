// Dinner controller that we use whenever we want to display detailed
// information for one dish
movieSearchApp.controller('RecommendedCtrl', function ($scope,$routeParams,Dinner) {
 

});

// or(var i in dishArray) {
//     priceSum += dishArray[i].MetricQuantity;